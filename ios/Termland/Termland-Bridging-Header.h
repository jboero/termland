// UniFFI generates this header with the Swift binding. Importing it through the
// app's bridging header keeps the exported Rust C ABI visible even when Xcode
// uses explicit Clang modules for an XCFramework build.
#include "TermlandCoreFFI.h"
